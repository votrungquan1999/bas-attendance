/**
 * Returns the href for the Basketball Utils tool selector landing page.
 * @returns The `/utils` route path
 */
export function utilsHomeHref(): string {
	return "/utils";
}

/**
 * Returns the href for the Countdown Timer tool.
 * @returns The `/utils/countdown-timer` route path
 */
export function countdownTimerHref(): string {
	return "/utils/countdown-timer";
}
